# 学习项目协作规则

这个文件给未来协作的 AI 助手使用。目标是把每个 AI 知识点都做成可讲、可看、可跑、可复盘的学习包。

## 输出风格

- 默认使用中文。
- 英文术语第一次出现时必须写成：中文名（英文名，白话：一句人话解释）。
- 不要只堆定义，要优先解释“它解决什么问题”。
- 每个公式都要配人话解释，必要时配图。
- 文档面向初学者，但不要把内容讲得虚。
- 代码里出现列表推导式、字典推导式、生成器表达式等压缩写法时，必须在旁边用注释写出等价的普通循环展开版。
- 代码里出现标准库函数、内置函数、对象方法、框架 API、特殊运行入口时，第一次出现必须解释来源、作用和为什么这里要用它。

## 新知识点目录标准

每个知识点放在：

```text
topics/序号-英文短名/
```

必须包含：

```text
README.md
checklist.md
slides.html
logic-map.html
demo-web/index.html
demo-project/
```

## 讲义标准

`slides.html` 是 PPT 式讲义，必须包含：

- 开头先讲目的和整体流程，再讲术语。
- 学这个知识点的原因。
- 技术栈选择原因。
- 关键术语的中文转义。
- 语法配套说明。
- 一个能和 demo 对上的逻辑图。
- 3 到 6 个适合动手改的参数。

## 演示网页标准

`demo-web/index.html` 必须是静态可打开页面。优先使用 HTML、CSS、JavaScript 自带能力，除非当前主题明显适合其他技术。

页面必须包含：

- 图形或动态图。
- 少量中文说明。
- 可交互控件，比如滑块、按钮、切换项。
- 不依赖登录、不依赖密钥。

## 练习题标准

如果给知识点制作练习题，使用 `learning-skills/quiz-builder/SKILL.md`。

题库必须包含：

- 出题原则。
- 来源参考。
- 即时反馈。
- 错题复盘。
- 分类筛选。
- 防泄题检查。

选择题正确答案不能全部显示为 A；小计算题输入框不能用正确答案当示例。

## 追问沉淀 hook

如果用户在学习过程中说“记一下”“放到笔记里”“做个 hook”“把这些追问整理一下”，使用 `learning-skills/question-note-hook/SKILL.md`。

默认把追问整理到当前主题的 `qa-notes.md`，不要逐字复制对话，要整理成能复习的知识点。

如果某个追问改变了学习路线或验收标准，同步更新当前主题 `README.md` 或 `checklist.md`。

## demo 项目标准

`demo-project/` 里的代码必须能独立运行。关键代码注释要写：

- 目的和流程：README 开头先说明这个 demo 要解决什么问题，以及训练过程按什么顺序发生。
- 目的：这段代码为什么存在。
- 语法：初学者容易卡在哪里。
- 连接：它对应逻辑图的哪一步。
- 展开：如果用了推导式或生成器表达式，要写等价普通循环。
- API：第一次出现的标准库函数、内置函数、对象方法、框架 API、特殊运行入口必须解释来源、作用和当前用途。

小主题优先写从零实现；复杂主题可以引用 GitHub 项目，但必须记录：

- 项目地址。
- 许可证。
- 选它的原因。
- 本学习包修改了哪些文件。

## 逻辑图标准

`logic-map.html` 要把概念流程和代码行号连起来。每个节点建议包含：

```text
节点名
人话解释
对应代码文件
对应行号
可动手改的参数
```

## 验收

完成一个主题后，至少运行一次 demo 项目，并把运行方式写进主题 `README.md`。
